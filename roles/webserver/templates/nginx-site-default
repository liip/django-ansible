server {
    listen {{ webserver_port }} default;
    server_name _;
    rewrite  ^/(.*)$  http://{{ full_domain }}/$1  permanent;
}

server {
    listen {{ webserver_port }};
    server_name {{ full_domain }};

{% include 'nginx-site-base' %}

}
